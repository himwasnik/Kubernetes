apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
  namespace: nginx
spec:
  resources:
    requests:
      storage: 500Mi
  volumeMode: Filesystem
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
